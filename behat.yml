# behat.yml

default:
    suites:
        graphs_storage:
            paths:    [ %paths.base%/features/graphs/storage ]
            filters: { role: user }
            contexts:
               - StorageUsageContext